# nextjs-nodejs-docker-cicd# Next.js + Node.js Docker CICD

## Structure
- `frontend`: Next.js application.
- `backend`: Node.js (Express) API.
- Dockerized for production.
- Jenkins pipeline for EC2 deployment.

## Run Locally
```bash
docker-compose up --build
